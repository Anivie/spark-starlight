[package]
name = "spark-starlight"
version = "0.1.0"
edition = "2021"

[dependencies]
spark-inference = { path = "../spark-inference" }
spark-media = { path = "../spark-media" }

ndarray = { version = "0.16.1", features = ["rayon"] }

anyhow = "1.0.86"